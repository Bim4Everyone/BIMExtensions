<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:UpdateSelectedLinks"
    Title="Витражи. Маркировка"
    mc:Ignorable="d"
    WindowStartupLocation="CenterScreen"
    Height="300" Width="500"
    MinHeight="300" MinWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition />
            <RowDefinition Height="45" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="Префикс маркировки"/>

        <TextBox
            Grid.Row="1"
            Margin="5"
            VerticalAlignment="Center"
            Text="{Binding marking_prefix, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock
            Grid.Row="2"
            Margin="5"
            VerticalAlignment="Center"
            Text="Начать нумерацию с номера:"/>

        <TextBox
            Grid.Row="3"
            Margin="5"
            VerticalAlignment="Center"
            Text="{Binding start_number, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock
            Grid.Row="4"
            Margin="5"
            VerticalAlignment="Center"
            Text="Суффикс маркировки"/>

        <TextBox
            Grid.Row="5"
            Margin="5"
            VerticalAlignment="Center"
            Text="{Binding marking_suffix, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel
            Grid.Row="7"
            Margin="10,5,10,10"
            Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <TextBlock
                Text="{Binding error_text}"
                Foreground="Orange"
                VerticalAlignment="Center"
                HorizontalAlignment="Right"/>
            <Button
                Width="100"
                Margin="10, 0"
                Content="Ок"
                Command="{Binding create_command}"
                Click="ButtonOK_Click"/>
            <Button
                Width="100"
                Content="Отмена"
                IsCancel="true"
                Click="ButtonCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>